# Normalize all text files to LF in the repo (matches Prettier endOfLine: "lf").
* text=auto eol=lf

# Lockfiles are generated — treat as text but never show in diffs by default.
pnpm-lock.yaml -diff
